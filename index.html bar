<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Cyber Virus — Simulasi</title>
<style>
  :root{
    --purple:#9d00ff;
    --white:#ffffff;
    --bg:#020202;
    --glow:#a100ff;
    --accent:#e11d48;
  }

  *{box-sizing:border-box}
  html,body{height:100%}
  body{
    margin:0;
    background: radial-gradient(1200px 800px at 50% 10%, #0a0a0a 0%, #000 60%, #000 100%);
    color:#ddd;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Courier New", monospace;
    overflow:hidden;
  }

  #bg, #scan {
    position:fixed; inset:0; width:100%; height:100%; display:block; z-index:-2;
  }
  #scan{
    z-index:-1;
    mix-blend-mode:screen;
    background: repeating-linear-gradient(
      to bottom,
      rgba(255,255,255,0.06) 0px,
      rgba(255,255,255,0.06) 1px,
      transparent 3px,
      transparent 6px
    );
    animation: scanMove 7s linear infinite;
    pointer-events:none;
  }
  @keyframes scanMove {
    0%{transform:translateY(-10%)}
    100%{transform:translateY(10%)}
  }

  .wrap{
    position:relative;
    width:min(1000px, 94vw);
    margin:0 auto;
    height:100%;
    display:grid;
    grid-template-rows:auto 1fr auto;
    place-items:center;
    padding:16px 0 24px;
    gap:10px;
  }

  /* TEKS BERGERAK PELANGI */
  .marquee{
    width:100%;
    overflow:hidden;
    white-space:nowrap;
    border-bottom:1px solid #222;
    border-top:1px solid #222;
    filter: drop-shadow(0 0 6px rgba(157,0,255,.45));
  }
  .marquee span{
    display:inline-block;
    padding-block:12px;
    font-weight:900;
    letter-spacing:1.5px;
    font-size:clamp(18px, 3.5vw, 34px);
    background: linear-gradient(90deg,
      violet, indigo, purple, magenta, violet
    );
    -webkit-background-clip:text;
    background-clip:text;
    color:transparent;
    animation: slide 16s linear infinite, rainbowGlow 3s ease-in-out infinite;
  }
  @keyframes slide {
    from{ transform: translateX(0) }
    to  { transform: translateX(-50%) }
  }
  @keyframes rainbowGlow {
    0%,100% { text-shadow: 0 0 6px violet, 0 0 12px purple; }
    50%     { text-shadow: 0 0 8px magenta, 0 0 16px violet; }
  }

  .panel{
    width:min(760px, 92vw);
    background: rgba(0,0,0,.55);
    border:1px solid #1a1a1a;
    border-radius:20px;
    backdrop-filter: blur(6px);
    box-shadow: 0 0 35px rgba(157,0,255,.12), inset 0 0 18px rgba(255,255,255,.02);
    padding:24px 18px 22px;
  }

  .photo-wrap{
    position:relative;
    width:min(360px, 70vw);
    aspect-ratio:1/1;
    margin-inline:auto;
    border-radius:50%;
    display:grid; place-items:center;
    background:
      radial-gradient(closest-side, rgba(157,0,255,.18), transparent 65%),
      radial-gradient(closest-side, rgba(255,0,255,.14), transparent 60%);
    box-shadow:
      0 0 40px rgba(157,0,255,.25),
      0 0 80px rgba(255,0,255,.15);
  }
  .ring{
    position:absolute; inset:6px;
    border-radius:50%;
    padding:10px;
    background: conic-gradient(from 0deg, var(--purple), var(--white), var(--purple));
    filter: drop-shadow(0 0 25px rgba(157,0,255,.35));
    animation: spin 12s linear infinite;
  }
  .ring::before{
    content:"";
    position:absolute; inset:6px; border-radius:50%;
    background:#000;
  }
  @keyframes spin { to{ transform: rotate(360deg) } }

  .photo{
    position:relative;
    z-index:2;
    width:92%;
    aspect-ratio:1/1;
    border-radius:50%;
    object-fit:cover;
    box-shadow: 0 0 16px rgba(255,255,255,.08);
    outline:3px solid rgba(255,255,255,.06);
  }

  .uploader{
    margin:18px auto 0;
    width:min(520px, 92%);
    border:1.5px dashed #383838;
    border-radius:14px;
    padding:12px;
    text-align:center;
    background:rgba(10,10,10,.6);
    transition:.2s ease;
  }
  .uploader.drag{ border-color:var(--purple); box-shadow:0 0 18px rgba(157,0,255,.25) inset }
  .uploader p{ margin:6px 0 10px; font-size:13px; color:#9aa; }
  .btn{
    appearance:none; border:1px solid #303030; border-radius:12px;
    background:linear-gradient(180deg,#101010,#0a0a0a);
    color:#eee; font-weight:700; padding:10px 14px; cursor:pointer;
    transition:.15s; font-size:14px;
  }
  .btn:hover{ transform:translateY(-1px); box-shadow:0 8px 22px rgba(157,0,255,.25) }
  .btn:active{ transform:translateY(0) }

  h1{
    margin:18px 0 6px;
    font-size:clamp(22px, 4vw, 36px);
    font-weight:900;
    letter-spacing:.12em;
    color:#eee;
    text-shadow: 0 0 12px rgba(157,0,255,.35);
  }
  .sub{
    margin:0 0 16px;
    color:#bdbdbd;
    font-weight:700;
  }

  .term{
    width:min(760px, 92vw);
    background:#060606;
    border:1px solid #181818;
    border-radius:16px;
    margin:10px auto 0;
    overflow:hidden;
    box-shadow: 0 0 24px rgba(157,0,255,.18);
  }
  .term-bar{
    display:flex; gap:8px; align-items:center;
    padding:8px 12px; background:#0b0b0b; border-bottom:1px solid #151515
  }
  .dot{width:10px; height:10px; border-radius:50%}
  .r{background:#ff5f56}.y{background:#ffbd2e}.g{background:#27c93f}
  .term-body{
    padding:14px;
    font-size:13px; line-height:1.5;
    color:#c8c8c8; min-height:120px;
  }
  .k{color:#d98bff} .v{color:#d0d0d0}
  .cursor{display:inline-block; width:8px; height:1.1em; background:#eee; margin-left:4px; animation:blink 1s step-end infinite}
  @keyframes blink{50%{opacity:0}}

  footer{font-size:12px; color:#9aa; opacity:.85}
</style>
</head>
<body>
<canvas id="bg"></canvas>
<div id="scan" aria-hidden="true"></div>

<div class="wrap">
  <div class="marquee" aria-label="running text">
    <span>&nbsp;HACKED BY AKBARZ666 • CYBER SIMULATION ONLY • KEEP SAFE • HACKED BY AKBARZ666 • CYBER SIMULATION ONLY • KEEP SAFE •</span>
  </div>

  <div class="panel">
    <div class="photo-wrap" aria-live="polite">
      <div class="ring"></div>
      <img id="photo" class="photo" src="https://images.unsplash.com/photo-1527980965255-d3b416303d12?q=80&w=600&auto=format&fit=crop" alt="Foto Anda"/>
    </div>

    <div id="drop" class="uploader">
      <p><strong>Drag & Drop</strong> foto ke sini atau</p>
      <button class="btn" id="pickBtn">Upload Foto</button>
      <input id="file" type="file" accept="image/*" hidden />
    </div>

    <h1>HACKED BY <span style="color:var(--purple)">AKBARZ666</span></h1>
    <div class="sub">This is only a <b>simulation</b> — for visual/edukasi.</div>
  </div>

  <div class="term" aria-hidden="false">
    <div class="term-bar">
      <div class="dot r"></div><div class="dot y"></div><div class="dot g"></div>
      <div style="margin-left:8px;color:#9aa;font-weight:700">/var/log/cyber/scan.log</div>
    </div>
    <div id="term" class="term-body"></div>
  </div>

  <footer>© 2025 <b>AKBARZ666</b> • Visual “virus cyber” ungu neon • GitHub Pages ready</footer>
</div>

<script>
(() => {
  const c = document.getElementById('bg');
  const ctx = c.getContext('2d');
  let W, H, particles;
  const rand = (a,b)=>Math.random()*(b-a)+a;

  function resize(){
    W = c.width = innerWidth * devicePixelRatio;
    H = c.height = innerHeight * devicePixelRatio;
    c.style.width = innerWidth+'px';
    c.style.height = innerHeight+'px';
    spawn();
  }
  function spawn(){
    const count = Math.min(120, Math.floor(innerWidth*innerHeight/11000));
    particles = new Array(count).fill(0).map(()=>({
      x: rand(0,W), y: rand(0,H),
      vx: rand(-0.25,0.25), vy: rand(-0.25,0.25),
      r: rand(1.5,3.2)*devicePixelRatio,
    }));
  }
  function tick(){
    ctx.clearRect(0,0,W,H);
    ctx.save();
    ctx.globalAlpha = .06;
    ctx.strokeStyle = '#9d00ff';
    ctx.lineWidth = 1*devicePixelRatio;
    const step = 40*devicePixelRatio;
    for(let x=0;x<W;x+=step){ ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,H); ctx.stroke(); }
    for(let y=0;y<H;y+=step){ ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(W,y); ctx.stroke(); }
    ctx.restore();
    particles.forEach(p=>{
      p.x += p.vx; p.y += p.vy;
      if(p.x<0||p.x>W) p.vx*=-1;
      if(p.y<0||p.y>H) p.vy*=-1;
      const grd = ctx.createRadialGradient(p.x,p.y,0,p.x,p.y,p.r*4);
      grd.addColorStop(0,'rgba(157,0,255,.9)');
      grd.addColorStop(1,'rgba(157,0,255,0)');
      ctx.fillStyle = grd;
      ctx.beginPath(); ctx.arc(p.x,p.y,p.r*3,0,Math.PI*2); ctx.fill();
      ctx.fillStyle = '#ffffff';
      ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill();
    });
    for(let i=0;i<particles.length;i++){
      for(let j=i+1;j<particles.length;j++){
        const a=particles[i], b=particles[j];
        const dx=a.x-b.x, dy=a.y-b.y;
        const d2=dx*dx+dy*dy;
        if(d2 < (120*devicePixelRatio)**2){
          ctx.strokeStyle='rgba(157,0,255,.12)';
          ctx.lineWidth=1*devicePixelRatio;
          ctx.beginPath(); ctx.moveTo(a.x,a.y); ctx.lineTo(b.x,b.y); ctx.stroke();
        }
      }
    }
    requestAnimationFrame(tick);
  }
  addEventListener('resize', resize);
  resize(); tick();
})();

(() => {
  const file = document.getElementById('file');
  const pickBtn = document.getElementById('pickBtn');
  const drop = document.getElementById('drop');
  const img = document.getElementById('photo');
  const loadFile = (f)=>{ if(f){ img.src = URL.createObjectURL(f); } };
  pickBtn.addEventListener('click', ()=> file.click());
  file.addEventListener('change', e => loadFile(e.target.files[0]));
  ['dragenter','dragover'].forEach(ev =>
    drop.addEventListener(ev, e => { e.preventDefault(); drop.classList.add('drag'); })
  );
  ['dragleave','drop'].forEach(ev =>
    drop.addEventListener(ev, e => { e.preventDefault(); drop.classList.remove('drag'); })
  );
  drop.addEventListener('drop', e => loadFile(e.dataTransfer.files?.[0]));
})();

(() => {
  const box = document.getElementById('term');
  const lines = [
    '[*] Initializing cyber-virus visual engine (purple mode)...',
    '[*] Spawning nodes: OK',
    '[*] Establishing mesh links: OK',
    '[*] Applying neon violet shield...',
    '[!] REMINDER: This is ONLY a simulation for learning.',
    '[*] Ready. Drop your image above.',
  ];
  let i=0, j=0;
  function type(){
    if(i>=lines.length){ box.insertAdjacentHTML('beforeend','<span class="cursor"></span>'); return; }
    if(j===0){ box.insertAdjacentHTML('beforeend', `<span class="k">$</span> <span class="v"></span><br/>`); }
    const v = box.querySelectorAll('.v')[i];
    v.textContent += lines[i][j++];
    box.scrollTop = box.scrollHeight;
    if(j>=lines[i].length){ i++; j=0; setTimeout(type, 500); }
    else{ setTimeout(type, 25); }
  }
  type();
})();
</script>
</body>
  </html>
